#include "yocto/gfx/image.hpp"

namespace yocto
{
    namespace gfx
    {

        image::format:: format(const char *id) : name(id)
        {
        }
        
        image::format:: ~format() throw()
        {
        }
        
        
    }
}
