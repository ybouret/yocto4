#include "yocto/lang/pattern.hpp"


namespace yocto
{
    namespace lang
    {
        pattern * pattern:: optimize(pattern *p) throw()
        {
            assert(p);
            
            return p;
        }
    }
}