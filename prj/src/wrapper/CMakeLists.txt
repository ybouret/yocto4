PROJECT(wrapper)

INCLUDE(${SDK}/share/yocto/swig.cmake)

IF(SWIG_FOUND)
	INCLUDE_DIRECTORIES(${CMAKE_CURRENT_SOURCE_DIR})
	IF(SWIG_PYTHON)
		SWIG_WRAPPER(sample python)
	ENDIF()
	IF(SWIG_TCL)
		SWIG_WRAPPER(sample tcl)
	ENDIF()
ENDIF()

